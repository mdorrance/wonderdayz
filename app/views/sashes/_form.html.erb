<%= form_for(@sash) do |f| %>
  <% if @sash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sash.errors.count, "error") %> prohibited this sash from being saved:</h2>

      <ul>
      <% @sash.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :award_id %><br />
    <%= f.number_field :award_id %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.number_field :person_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
