<%= form_for(@person_avatar) do |f| %>
  <% if @person_avatar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@person_avatar.errors.count, "error") %> prohibited this person_avatar from being saved:</h2>

      <ul>
      <% @person_avatar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :avatar_id %><br />
    <%= f.number_field :avatar_id %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br />
    <%= f.number_field :person_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
