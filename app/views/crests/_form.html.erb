<%= form_for(@crest) do |f| %>
  <% if @crest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crest.errors.count, "error") %> prohibited this crest from being saved:</h2>

      <ul>
      <% @crest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
