<%= form_for(@family_blog) do |f| %>
  <% if @family_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@family_blog.errors.count, "error") %> prohibited this family_blog from being saved:</h2>

      <ul>
      <% @family_blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :family_id, :value => current_user.family_id %>
  </div>
  <div class="field">
    <%= f.label :blog %>
    <%= f.text_area :blog %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
