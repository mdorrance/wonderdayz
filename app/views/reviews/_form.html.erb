<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :adventure %> <%= f.collection_select :adventure_id, Adventure.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.label :good %><br />
    <%= f.text_area :good %>
  </div>
  <div class="field">
    <%= f.label :bad %><br />
    <%= f.text_area :bad %>
  </div>
  <div class="field">
    <%= f.hidden_field :person_id, :value => current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
